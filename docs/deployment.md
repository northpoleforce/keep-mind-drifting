# Deployment (Draft)

- Frontend: Vercel (`apps/web`)
- Backend: Railway or Render (`apps/api`)
- Memory: existing online Evermemos service

Required envs:

- `EVERMEMOS_BASE_URL`
- `EVERMEMOS_API_KEY`
- `LLM_BASE_URL`
- `LLM_API_KEY`
